services:
  ecommerce-fe:
    container_name: ecommerce-fe
    image: ecommerce-frontend:v1
    build:
      context: ./ecommerce-fe/
    restart: always
    ports:
      - "3001:80"
    networks:
      - net

  broker-service:
    container_name: broker-service
    image: broker-service:v1
    build:
      context: ./services/broker-service
    restart: always
    ports:
      - "8080:8080"
    environment:
      AUTH_SERVICE_HOST: ${AUTH_SERVICE_HOST}
      USER_SERVICE_HOST: ${USER_SERVICE_HOST}
      PRODUCT_SERVICE_HOST: ${PRODUCT_SERVICE_HOST}
      CART_SERVICE_HOST: ${CART_SERVICE_HOST}
      CHECKOUT_SERVICE_HOST: ${CHECKOUT_SERVICE_HOST}
    networks:
      - net

  # Cart Service
  cart-service:
    container_name: cart-service
    image: cart-service:v1
    build:
      context: ./services/cart-service
    restart: always
    ports:
      - "50054:50054"
    depends_on:
      - redis-cart
    networks:
      - net

  # Redis for Cart Service
  redis-cart:
    container_name: redis-cart
    image: "redis:alpine"
    ports:
      - "6379:6379"
    volumes:
      - ./db-data/redis-cart:/data
      - ./services/cart-service/internal/repository/redis/db:/docker-entrypoint-initdb.d
    restart: always
    entrypoint: >
      sh -c "
        redis-server --appendonly yes &
        sleep 5 &&
        chmod +x /docker-entrypoint-initdb.d/02-initdb.sh &&
        /docker-entrypoint-initdb.d/02-initdb.sh &&
        wait
      "
    networks:
      - net

  # Checkout Service
  checkout-service:
    container_name: checkout-service
    image: checkout-service:v1
    build:
      context: ./services/checkout-service
    restart: always
    ports:
      - "50055:50055"
    depends_on:
      - mongo-checkout
    networks:
      - net

  # MongoDB for Checkout Service
  mongo-checkout:
    container_name: mongo-checkout
    image: "mongo:5"
    ports:
      - "27019:27017"
    environment:
      MONGO_INITDB_DATABASE: ${MONGO_DATABASE_CHECKOUT}
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_USERNAME_CHECKOUT}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_PASSWORD_CHECKOUT}
    volumes:
      - ./db-data/mongo-checkout:/data/db
      - ./services/checkout-service/internal/repository/mongodb/db:/docker-entrypoint-initdb.d
    command: [ "--logpath", "/var/log/mongodb/mongod.log", "--logappend" ]
    networks:
      - net

  authentication-service:
    container_name: authentication-service
    image: authentication-service:v1
    build:
      context: ./services/authentication-service
    restart: always
    ports:
      - "50051:50051"
    depends_on:
      - postgres-auth
    networks:
      - net

  postgres-auth:
    container_name: postgres-auth
    image: "postgres:14.0"
    ports:
      - "5435:5432"
    restart: always
    environment:
      POSTGRES_USER: ${AUTH_DB_USER}
      POSTGRES_PASSWORD: ${AUTH_DB_PASSWORD}
      POSTGRES_DB: ${AUTH_DB_NAME}
    volumes:
      - ./db-data/postgres-auth:/var/lib/postgresql/data/
      - ./services/authentication-service/scripts/db:/docker-entrypoint-initdb.d
    networks:
      - net

  user-service:
    container_name: user-service
    image: user-service:v1
    build:
      context: ./services/user-service
    restart: always
    ports:
      - "50052:50052"
    depends_on:
      - postgres-users
    networks:
      - net

  postgres-users:
    container_name: postgres-users
    image: "postgres:14.0"
    ports:
      - "5434:5432"
    restart: always
    environment:
      POSTGRES_USER: ${USER_DB_USER}
      POSTGRES_PASSWORD: ${USER_DB_PASSWORD}
      POSTGRES_DB: ${USER_DB_NAME}
    volumes:
      - ./db-data/postgres-users:/var/lib/postgresql/data/
      - ./services/user-service/scripts/db:/docker-entrypoint-initdb.d
    networks:
      - net

  product-service:
    container_name: product-service
    image: product-service:v1
    build:
      context: ./services/product-service
    restart: always
    ports:
      - "8082:8082"
      - "50053:50053"
    environment:
      DSN: ${PRODUCT_DSN}
    depends_on:
      - postgres-products
    networks:
      - net

  postgres-products:
    container_name: postgres-products
    image: "postgres:14.0"
    ports:
      - "5433:5432"
    restart: always
    environment:
      POSTGRES_USER: ${PRODUCT_DB_USER}
      POSTGRES_PASSWORD: ${PRODUCT_DB_PASSWORD}
      POSTGRES_DB: ${PRODUCT_DB_NAME}
    volumes:
      - ./db-data/postgres-products:/var/lib/postgresql/data/
      - ./services/product-service/internal/repository/postgres/db:/docker-entrypoint-initdb.d
    networks:
      - net

  logger-service:
    container_name: logger-service
    image: logger-service:v1
    build:
      context: ./services/logger-service
    restart: always
    ports:
      - "50056:50056"
    depends_on:
      - mongo-logger
    networks:
      - net

  mongo-logger:
    image: "mongo:4.2.16-bionic"
    container_name: mongo-logger
    ports:
      - "27018:27017"
    environment:
      MONGO_INITDB_DATABASE: ${MONGO_DATABASE_LOGGER}
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_USERNAME_LOGGER}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_PASSWORD_LOGGER}
    volumes:
      - ./db-data/mongo/:/data/db
    networks:
      - net

  mail-service:
    container_name: mail-service
    image: mail-service:v1
    build:
      context: ./services/mail-service
    restart: always
    ports:
      - "50057:50057"
    environment:
      MAIL_DOMAIN: ${MAIL_DOMAIN}
      MAIL_HOST: ${MAIL_HOST}
      MAIL_PORT: ${MAIL_PORT}
      MAIL_ENCRYPTION: ${MAIL_ENCRYPTION}
      MAIL_USERNAME: ${MAIL_USERNAME}
      MAIL_PASSWORD: ${MAIL_PASSWORD}
      FROM_NAME: ${FROM_NAME}
      FROM_ADDR: ${FROM_ADDR}
    networks:
      - net

  listener-service:
    container_name: listener-service
    image: listener-service:v1
    build:
      context: ./services/listener-service
    restart: always
    depends_on:
      - rabbitmq
      - mail-service
    networks:
      - net

  rabbitmq:
    container_name: rabbitmq
    image: "rabbitmq:3.9-alpine"
    ports:
      - "5672:5672"
    volumes:
      - ./db-data/rabbitmq/:/var/lib/rabbitmq/
    networks:
      - net

networks:
  net:
    driver: bridge
