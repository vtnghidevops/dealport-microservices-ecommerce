apiVersion: apps/v1
kind: Deployment
metadata:
  name: ecommerce-cart-development
  namespace: ecommerce
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ecommerce-cart-app
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 2
      maxSurge: 2
  template:
      metadata:
        labels:
          app: ecommerce-cart-app
      spec:
        initContainers:
          - name: wait-for-redis-cart-db
            image: busybox:1.28
            command: ['sh', '-c', 'until nslookup redis-cart.ecommerce.svc.cluster.local; do echo waiting for redis; sleep 2; done;']
        containers:
          - name: ecommerce-cart-app
            image: tannghidevops/cart-service:v2
            ports: 
              - containerPort: 50054
                name: tcp
                protocol: TCP
            resources:
              requests:
                cpu: "125m"
                memory: "250Mi"
              limits:
                cpu: "250m"
                memory: "500Mi"
            envFrom:
              - secretRef:
                  name: ecommerce-cart-secret
        # imagePullSecrets:
        #   - name: auth-registry
        

