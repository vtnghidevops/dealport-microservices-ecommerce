apiVersion: v1
kind: Service
metadata:
  name: mongo-logger
  namespace: ecommerce
  labels:
    app: mongo-logger
spec:
  ports:
  - port: 27017
    targetPort: 27017
    protocol: TCP
    name: mongo
  selector:
    app: mongo-logger
  type: ClusterIP
  sessionAffinity: None 