apiVersion: apps/v1
kind: Deployment
metadata:
  name: ecommerce-logger-development
  namespace: ecommerce
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ecommerce-logger-app
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 2
      maxSurge: 2
  template:
      metadata:
        labels:
          app: ecommerce-logger-app
      spec:
        initContainers:
          - name: wait-for-mongo-logger-db
            image: busybox:1.28
            command: ['sh', '-c', 'until nslookup mongo-logger.ecommerce.svc.cluster.local; do echo waiting for mongo; sleep 2; done;']
        containers:
          - name: ecommerce-logger-app
            image: tannghidevops/logger-service:v2
            ports: 
              - containerPort: 50056
                name: tcp
                protocol: TCP
            resources:
              requests:
                cpu: "125m"
                memory: "250Mi"
              limits:
                cpu: "250m"
                memory: "500Mi"
            envFrom:
              - secretRef:
                  name: ecommerce-logger-secret
        # imagePullSecrets:
        #   - name: auth-registry
        

