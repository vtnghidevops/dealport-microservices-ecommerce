apiVersion: v1
kind: Service
metadata:
  name: postgres-auth
  namespace: ecommerce
  labels:
    app: postgres-auth
spec:
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgres
  selector:
    app: postgres-auth
  type: ClusterIP
  sessionAffinity: None 