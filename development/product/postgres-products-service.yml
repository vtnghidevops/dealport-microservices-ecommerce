apiVersion: v1
kind: Service
metadata:
  name: postgres-products
  namespace: ecommerce
  labels:
    app: postgres-products
spec:
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgres
  selector:
    app: postgres-products
  type: ClusterIP
  sessionAffinity: None 