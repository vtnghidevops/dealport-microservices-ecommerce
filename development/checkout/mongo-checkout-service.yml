apiVersion: v1
kind: Service
metadata:
  name: mongo-checkout
  namespace: ecommerce
  labels:
    app: mongo-checkout
spec:
  ports:
  - port: 27017
    targetPort: 27017
    protocol: TCP
    name: mongo
  selector:
    app: mongo-checkout
  type: ClusterIP
  sessionAffinity: None 